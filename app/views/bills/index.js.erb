// it will differentiate between admin and user bills for ajax calling
<% if current_user.role_id==1  %>
$("#admin_page").html("<%= escape_javascript(render( "index")) %>");
<% else %>
$("#user_page").html("<%= escape_javascript(render("index")) %>");
<% end %>