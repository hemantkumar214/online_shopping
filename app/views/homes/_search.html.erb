<!-- This page will search records according user keyboard -->
<div class="container" style="margin-top: 70px;">
	<div class="row">
		<div class="col-md-2" style="margin-top: 13px;"></div>

		<!-- display searching data   -->
		<div class="col-md-8" style="margin-top: 13px;">
			<div>
				<% if @category_results == [] && @product_results == [] %>
					<div class="alert alert-warning"><h2> Please search relevent Keyboard. </h2></div>
				<% end %>
			</div>

			<div class="nav-section-cat-list">
				<% if @category_results != [] && @product_results == [] %>
					<% if @root_child_categories %>
						<% @root_child_categories.each do |category| %>
							<div class="slide fltLeft" style="height: 190px;">
								<%= link_to(url_for("subcategory/#{category.name}"),:remote => true) do %>
										<div class="fltLeft">
											<img class="imageSize" src="<%= category.image %>">
										</div>
										<div class=" labelSize" style="position:relative; top:10px; left:60px;">
											<span class="arrow-right"><b><%= category.name %></b></span>
										</div>
									<% end %>
							</div>
						<% end %>
					<% end %>
				<% end %>
			</div>
		</div>

		<div class="col-md-2" style="margin-top: 13px;">
		</div>
	</div>
</div>